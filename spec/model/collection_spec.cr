require "../spec_helper"
require "../data/example_models"

module CollectionSpec
  describe Clear::Collection do
    pending "[]"
    pending "[]?"

    pending "find" do
    end

    pending "first_or_create" do
    end

    pending "first_or_build" do
    end


    pending "first" do
    end

    pending "last" do
      # last, last!, last(fetch_columns)
    end

    pending "delete_all" do
    end
  end
end