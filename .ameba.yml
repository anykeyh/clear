# This configuration file was generated by `ameba --gen-config`
# on 2018-12-23 13:17:47 +07:00 using Ameba version 0.7.0.
# The point is for the user to remove these configuration records
# one by one as the reported problems are removed from the code base.

# Problems found: 8
# Run `ameba --only LargeNumbers` for details
LargeNumbers:
  Description: Disallows usage of large numbers without underscore
  IntMinDigits: 5
  Enabled: true
  Excluded:
  - spec/model/model_spec.cr

# Problems found: 7
# Run `ameba --only UselessAssign` for details
UselessAssign:
  Description: Disallows useless variable assignments
  Enabled: true
  Excluded:
  - spec/model/model_spec.cr
  - src/clear/error_messages.cr
  - src/clear/extensions/full_text_searchable/model.cr
  - src/clear/model/modules/has_columns.cr

# Problems found: 25
# Run `ameba --only TrailingWhitespace` for details
TrailingWhitespace:
  Description: Disallows trailing whitespaces
  Enabled: true
  Excluded:
  - src/clear/error_messages.cr
  - src/clear/model/event_manager.cr
  - src/clear/model/model.cr
  - src/clear/model/modules/relations/belongs_to_macro.cr
  - src/clear/model/modules/relations/has_one_macro.cr
  - src/clear/model/modules/has_saving.cr
  - src/clear/migration/operation/execute.cr
  - src/clear/migration/migration.cr

# Problems found: 1
# Run `ameba --only ShadowedArgument` for details
ShadowedArgument:
  Description: Disallows shadowed arguments
  Enabled: true
  Excluded:
  - src/clear/sql/query/join.cr

# Problems found: 1
# Run `ameba --only PredicateName` for details
PredicateName:
  Description: Disallows tautological predicate names
  Enabled: true
  Excluded:
  - src/clear/sql/query/on_conflict.cr
